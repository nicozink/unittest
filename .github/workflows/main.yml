name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Configure CMake
      run: |
        mkdir -p build
        cd build
        cmake ..
        make
    - name: Run Unit Tests
      run: |
        unittest_test
